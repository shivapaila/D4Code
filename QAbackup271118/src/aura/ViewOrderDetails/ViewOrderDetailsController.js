({
    doInit : function(component, event, helper) {
        helper.loadOrder(component, helper);
    }
    //NOTE - requirement is for the order line itemt to display
    // but to _NOT_ click through to the product page 
    // per Lilly 2/2/2018 - ljo
})